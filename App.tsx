/**
 * When `expo` is hoisted to the workspace root, `expo/AppEntry.js` does
 * `import App from '../../App'` relative to `node_modules/expo` → this file.
 */
export { default } from "./apps/mobile/App";
